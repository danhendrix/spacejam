import Questions from './questions';

const mathQuestions = [
    {
        question: 'What is 2 + 2?',
        options: [3, 8, 4],
        answer: 4,
    },
];

const Math = new Questions(mathQuestions);

export default Math;
