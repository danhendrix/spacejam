import Questions from './questions';

const mathQuestions = [
    {
        question: 'What is 5 + 2?',
        options: [3, 7, 4],
        answer: 7,
    },
];

const Math = new Questions(mathQuestions);

export default Math;
